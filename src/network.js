const net = require('net');

/**
 * @class Client
 * The TCP client that connects to the server
 */
class Client {
    constructor() {
        this.server = net.createServer((server) => {
            
        });
    }
}
